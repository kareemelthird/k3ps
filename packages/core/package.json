{
  "name": "@ps/core",
  "version": "0.0.0",
  "private": true,
  "description": "Pure, framework-free domain logic: pricing engine, money (piastres), time (Cairo TZ), inventory ledger, shared types. No React, no Supabase imports.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "dayjs": "^1.11.13"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.3"
  }
}
