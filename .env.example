# Copy to .env (gitignored). Never commit real secrets.

# Supabase (per environment)
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=          # server-only (web/super-admin, edge functions) — never ship to clients
SUPABASE_PROJECT_REF=

# Stripe (Phase 9 — SaaS billing)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# App
APP_TZ=Africa/Cairo
