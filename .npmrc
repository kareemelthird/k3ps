# Expo SDK 53 pulls transitive peers (e.g. @expo/metro-runtime -> react-native@*)
# that npm's strict peer resolver rejects inside an npm workspace. Expo officially
# supports installing with legacy peer resolution; pin it so local + CI agree.
legacy-peer-deps=true
