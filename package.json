{
  "name": "ps-managment",
  "version": "0.0.0",
  "private": true,
  "description": "Multi-tenant SaaS for gaming cafes (PlayStation lounges). Monorepo: shared core + Expo mobile + Next.js web + Supabase backend.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20.19.4"
  },
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "verify": "echo \"Use the ps-verify skill: typecheck + test + expo export + next build\""
  },
  "packageManager": "npm@10.0.0",
  "devDependencies": {
    "supabase": "^2.107.0"
  },
  "overrides": {
    "react-is": "^19.0.0"
  }
}
